[
    {{- range $index, $element := where site.RegularPages "Type" "in" site.Params.mainSections -}}
    {{ if $index }},{{ end }}
    {    
        "title": "{{ .Title }}",
        "url": "{{ .RelPermalink }}",
        "body": {{ .Plain | jsonify }}
    }
    {{- end }}
]
