[
    {{- range $index, $element := .Site.RegularPages -}}
    {{ if $index }},{{ end }}
    {    
        "title": "{{ .Title }}",
        "url": "{{ .RelPermalink }}",
        "body": {{ .Plain | jsonify }},
    }
    {{- end }}
]
